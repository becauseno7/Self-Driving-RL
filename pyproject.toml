[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "self-driving-rl"
version = "0.6.0"
description = "A learning-focused reinforcement learning project for simulated driving."
readme = "README.md"
requires-python = ">=3.11,<3.13"
dependencies = [
    "gymnasium==1.3.0",
    "highway-env==1.12.0",
    "numpy==2.3.2",
    "pygame-ce==2.5.7",
    "sb3-contrib==2.9.0",
    "stable-baselines3==2.9.0",
    "tensorboard==2.20.0",
    "torch==2.13.0",
]

[project.optional-dependencies]
dev = [
    "pytest==8.4.1",
    "ruff==0.12.7",
]

[project.scripts]
sdr-game = "self_driving_rl.game:main"
sdr-rlaif = "self_driving_rl.rlaif:main"
sdr-dagger = "self_driving_rl.dagger:main"
sdr-inspect = "self_driving_rl.inspect_environment:main"
sdr-random = "self_driving_rl.random_baseline:main"
sdr-train = "self_driving_rl.train:main"
sdr-evaluate = "self_driving_rl.evaluate:main"

[tool.hatch.build.targets.wheel]
packages = ["src/self_driving_rl"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.uv.sources]
torch = { index = "pytorch-cu126" }

[[tool.uv.index]]
name = "pytorch-cu126"
url = "https://download.pytorch.org/whl/cu126"
explicit = true

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
